// export interface JwtPayload {
//     id: number;
//     fullname: string;
//     // Add other properties that are part of your JWT payload
//   }
  // src/auth/jwt-payload.interface.ts
export interface JwtPayload {
  id: number; // Adjust type if needed
}
